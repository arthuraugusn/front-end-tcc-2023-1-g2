import { Button, Modal, Typography } from "@mui/material";
import { Box } from "@mui/system";
import { ModalPadrao } from "..";

export const ModalExcluirPerfil = ({ props }) => {
  return <ModalPadrao props={props} />;
};
