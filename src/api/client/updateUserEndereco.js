import api from "../api";

export const updateUserEndereco = (idEndereco, idUsuario, setResponseError) => {
  /* api.put(`enderecoUsuario/${idUsuario}`,{
    id_endereco:
  }).then((response) => {
    setResponseError(response);
  }); */
};
